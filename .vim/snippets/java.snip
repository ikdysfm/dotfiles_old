snippet if
abbr if
  if (${1}) {
    ${2}
  }

snippet elif
abbr else if
  else if (${1}) {
    ${2}
  }

snippet wh
abbr while
  while (${1}) {
    ${2}
  }

snippet for
abbr for
  for (${1}; ${2}; ${3}) {
    ${4}
  }

snippet fore
abbr for each
  for (${1} : ${2}) {
  ${3}
  }
